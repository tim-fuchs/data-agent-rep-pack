# Version Control Recommendation

After each logical task chunk:

1. Check whether edited files are in a Git environment.
2. If Git is unavailable, ask once per session whether a Git environment should be created (options: Yes, No).
3. Ask whether edits should be committed.
