# Information Source Priority

When answering requests that need external information, use this order until relevant information is found:

1. Project context
2. MCP server `grounded-docs`
3. MCP server `github-mcp` (focus on `European-XFEL` organization)
4. MCP server `gitlab-mcp`
5. `webfetch`
